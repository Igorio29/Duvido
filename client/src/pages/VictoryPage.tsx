import { Lives } from "../components/Lives";
import { Logo } from "../components/Logo";
import type { Player } from "../types/game";
export function VictoryPage({ players, onReset }: { players:Player[]; onReset:()=>void }) { const winner = players.find(p=>p.lives>0)!; return <main className="relative z-10 mx-auto flex min-h-screen max-w-xl items-center px-4 py-8"><section className="w-full rounded-[2rem] border-2 border-ink bg-white p-7 text-center shadow-pop sm:p-10"><Logo/><div className="my-6 text-7xl animate-bounce">🏆</div><h1 className="text-3xl font-black sm:text-4xl">{winner.name} venceu!</h1><p className="mt-2 font-bold text-ink/55">Conhecimento? Coragem? Talvez os dois.</p><div className="my-7 overflow-hidden rounded-2xl border-2 border-ink"><div className="bg-ink py-2 text-xs font-black uppercase tracking-widest text-white">Placar final</div>{players.map((p,i)=><div key={p.id} className={`flex items-center justify-between px-5 py-4 ${i<players.length-1?"border-b-2 border-ink":""}`}><strong>{p.name}</strong><Lives count={p.lives}/></div>)}</div><button onClick={onReset} className="w-full rounded-2xl border-2 border-ink bg-lime p-4 font-black shadow-[0_5px_0_#201a38] transition active:translate-y-1 active:shadow-none">JOGAR NOVAMENTE</button></section></main>; }
